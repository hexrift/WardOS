# WardOS capability policy for ward-demo. Narrows the user default: this project
# needs no network and no nested containers. Filesystem, credentials and observer
# inherit the secure defaults (worktree rw, host denied, cloud denied).
network: localhost_only
containers: none
