Placeholder content for the ward-bench ci-subset fixture (#150). Kept
deliberately tiny: this fixture's point is to be a fast, pinned worktree for
timing sandbox start, `ward status` and verifier spawn, not a realistic
project size.
