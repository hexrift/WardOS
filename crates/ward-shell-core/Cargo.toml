[package]
name = "ward-shell-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "WardOS: the Ward Shell view model (trust bar, observer feed, launcher, settings), toolkit-free"

[lib]
name = "ward_shell_core"
path = "src/lib.rs"

[dependencies]
ward-daemon = { path = "../ward-daemon", version = "0.2.0" }
ward-events = { path = "../ward-events", version = "0.2.0" }
ward-policy = { path = "../ward-policy", version = "0.2.0" }
serde = { workspace = true }

[lints]
workspace = true
